# speech to text and text to speech
realtimestt==0.3.104
realtimetts[kokoro,coqui,orpheus]==0.5.5

# Fix Coqui TTS compatibility issue with transformers
# transformers >= 4.42.0 removed BeamSearchScorer which Coqui TTS depends on
transformers==4.47.0

# webserver dependencies
fastapi
uvicorn

# configuration
python-dotenv

# llm providers
ollama
openai